<% if target.errors.any? %>
<div class="error_explanation">
  <span><%= pluralize(target.errors.count, "error") %> prevented this record from being saved:</span>
  <ul>
    <% target.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>